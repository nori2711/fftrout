<link href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/css/lightbox.css" rel="stylesheet">
<script src="https://code.jquery.com/jquery-1.12.4.min.js" type="text/javascript"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/js/lightbox.min.js" type="text/javascript"></script>

<div class="posting_form top_content">
  <h2>釣果マップ</h2></br>

  <style>
    #map {
      width: 97%;
      height: 400px;
      background-color: grey;
      margin: 5px auto 20px auto;
    }
  </style>
  <div id="map"></div>

  <%= search_form_for @search do |f| %>
    <div>
      <%= f.label "検索開始日", style: 'color: #828282' %>
      <%= f.date_field :created_at_gteq, class: "form-control" %>
      <%= f.label "終了日", style: 'color: #828282; margin: 7px 0 5px 0' %>
      <%= f.date_field :created_at_lteq, class: "form-control" %>
    </div>
    <div class="action">
      <%= f.submit "検索", class: "btn btn--block" do %>
      <% end %>
    </div>
  <% end %>

  <% @riverpoints.each do |river| %>
    <%= river.riverpoint_number %>：<%= river.riverpoint_name %> <%= link_to ">絞込み", "/users.#{current_user.id}?riverpoint_latitude=#{river.riverpoint_latitude}&riverpoint_longitude=#{river.riverpoint_longitude}", method: :get %></br>
  <% end %>
</div>

<div style="font-size: 12px; color: #828282">
  ＜検索について＞</br>
  日付の基準は00:00です</br>
  2018/8/1の1日だけ検索する場合は2018/8/1〜2018/8/2と入力</br>
  日付検索の結果を場所で絞り込めます（周辺約200m）</br>
  場所検索の結果を日付で絞り込むことはできません</br>
  </br>
</div>

<div class="hunt_content">
  <ul>
    <% @hunts.each do |hunt| %>
      <li>
        <%= time_ago_in_words hunt.created_at %>前：<%= hunt.user.nickname %> <%= link_to ">マップ表示", "/hunts/#{hunt.id}", method: :get %></br>
        <a class="photo" href= <%= hunt.fish_photo.url %> data-lightbox="abc" data-title="魚写真">
          <%= image_tag hunt.fish_photo.url, alt: '',size: '60' %>
        </a>
        <a class="photo" href= <%= hunt.fly_photo.url %> data-lightbox="abc" data-title="Fly写真">
          <%= image_tag hunt.fly_photo.url, alt: '',size: '60' %>
        </a>
        <a class="photo" href= <%= hunt.spot_photo.url %> data-lightbox="abc" data-title="場所写真">
          <%= image_tag hunt.spot_photo.url, alt: '',size: '60' %>
        </a>
      </li>
    <% end %>
  </ul>
</div>

<script>
  function initMap() {
    var yozawa = {lat:35.751834, lng:139.179959};

    var map = new google.maps.Map(document.getElementById('map'), {
      zoom: 14,
      center: yozawa
    });

    setMarkers(map);
  }

  var hunts_lat = <%= @hunt_lat %>;
  var hunts_lng = <%= @hunt_lng %>;
  var riverpoints_lat = <%= @riverpoint_lat %>;
  var riverpoints_lng = <%= @riverpoint_lng %>;
  // rawメソッドで配列「"」のエスケープ処理をおこなわない
  var riverpoints_num = <%= raw @riverpoint_num %>;

  // マーカー情報の作成
  function setMarkers(map) {
    for (var i = 0; i < riverpoints_lat.length; i++) {
      var riverpoint_lat = riverpoints_lat[i];
      var riverpoint_lng = riverpoints_lng[i];
      var riverpoint_num = riverpoints_num[i];
      var marker = new google.maps.Marker({
        position: {lat: riverpoint_lat, lng: riverpoint_lng},
        map: map,
        icon: {
          fillColor: "#000000",    //塗り潰し色
          fillOpacity: 0.5,        //塗り潰し透過率
          path: google.maps.SymbolPath.BACKWARD_CLOSED_ARROW, //マーカーを指定
          scale: 3,                //マーカーのサイズ
          strokeColor: "#ffffff",  //枠の色
          strokeWeight: 0.4        //枠の透過率
        },
        label: {
          text: riverpoint_num,
          color: "#5f5f5f",
          fontSize: "1em"
        }
      });
    }
    for (var i = 0; i < hunts_lat.length; i++) {
      var hunt_lat = hunts_lat[i];
      var hunt_lng = hunts_lng[i];
      var marker = new google.maps.Marker({
        position: {lat: hunt_lat, lng: hunt_lng},
        map: map,
        icon: {
          fillColor: "#FF0000",    //塗り潰し色
          fillOpacity: 0.5,        //塗り潰し透過率
          path: google.maps.SymbolPath.CIRCLE, //円を指定
          scale: 4,                //円のサイズ
          strokeColor: "#ffffff",  //枠の色
          strokeWeight: 1.0        //枠の透過率
        },
      });
    }
  }
</script>

<script async defer src="https://maps.googleapis.com/maps/api/js?key=<%= @apikey %>&callback=initMap">
</script>
